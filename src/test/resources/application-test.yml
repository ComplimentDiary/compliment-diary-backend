spring:
  config:
    activate:
      on-profile: test
  application:
    name: compliment-diary
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
jasypt:
  encryptor:
    password: compliment_password
    algorithm: PBEWithMD5AndDES